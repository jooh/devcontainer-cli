[package]
name = "devcontainer"
version = "0.0.1"
edition = "2021"
description = "Native Rust foundation for devcontainer CLI"
license = "MIT"
publish = false

[dependencies]
serde_json = "1.0"
serde_yaml = "0.9"
