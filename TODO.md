# TODO

## Near-term follow-ups

- Expand repo-owned parity coverage beyond the current `read-configuration` scenario.
- Add more integration coverage for command error paths and collection flows.
- Improve interactive `exec`/TTY fidelity and document any remaining gaps.
- Replace placeholder `features`/`templates` publish behavior with real registry flows when that work is resumed.
- Decide whether any remaining upstream-oriented docs under `docs/features/` and `docs/templates/` should be rewritten for native-only behavior or explicitly marked as upstream reference material.
